//用img将div填充满，裁剪
function sizeAdapte(imgDivId, imgId) {
	var destWidth, destHeight;

	//文档宽高
	var w = Math.max(document.body.scrollWidth, document.documentElement.scrollWidth);
	var h = Math.max(document.body.scrollHeight, document.documentElement.scrollHeight);

	var iw = $("imgId").css("width");
	var ih = $("imgId").css("height");
	alert(iw)

//	if(iw / ih < w / h) {
//		var ratio = w / iw;
//		destWidth = ratio *
//	}
//	img.style.width = destWidth; 
//	img.style.height = destHeight;
} 